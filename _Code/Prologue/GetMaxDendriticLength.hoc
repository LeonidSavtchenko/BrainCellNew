
// Get the maximum dendritic length
// Input: dendrite_ref (taken from the top level)
// Returns the max length
func getMaxDendriticLength() { local maxDendLen
    maxDendLen = -1
    for eachSecInList(dendrite_ref) {
        maxDendLen = math.max(maxDendLen, L)
    }
    return maxDendLen
}
